---
user_home: "/home/{{user_name}}"

base_packages:
  - tmux
  - bash-completion
  - git
  - ranger

env_packages:
  server: []
  desktop:
    - networkmanager
    - network-manager-applet
    - shadowsocks-libev
    - proxychains

    - xorg-xmodmap
    - xorg-xrandr
    - arandr
    - feh

    - dex

common_packages: >
  {{base_packages + env_packages[env]}}
